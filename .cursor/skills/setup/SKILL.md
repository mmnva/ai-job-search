---
name: setup
description: >-
  Cursor mirror of /setup — populate candidate profile placeholders in CLAUDE.md
  and related setup files. Use when onboarding this job-search fork or filling
  [PLACEHOLDER] profile fields.
---

# Setup (Cursor adapter)

Follow the canonical setup flow in `.claude/commands/setup.md` (and any linked setup skill docs under `.claude/`).

This fork's market defaults are **United States** (see `CLAUDE.md` Market section and `.claude/skills/job-scraper/search-queries.md`). Do not invent personal bio facts — ask the user for name, location, experience, and skills.
