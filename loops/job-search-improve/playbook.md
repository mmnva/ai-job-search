# PLAYBOOK

## How to pick the next thing to try

- Prefer small changes that can be measured independently via `./score.sh`.
- Try the most likely score improvement first.

## Directions to explore

- Keep US portal enablement and scrape-router ladder markers intact (contract tests).
- Deepen Cursor thin adapters only when `.claude/` SoT already covers the workflow.
- Wire resume-tailoring / interview-prep docs without inventing candidate bio facts.
- Improve `score.sh` coverage only by adding deterministic checks — never LLM judgment.

## Do not try

- Deleting `.claude/` or widening Bash allowlist without security_guards + tests.
- Live LinkedIn scraping inside the verifier.
- Editing `score.sh` from the coach role.
